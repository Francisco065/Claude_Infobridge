# HTTP assíncrono (chamadas à Multiportal)
httpx==0.27.0

# Banco de Dados
asyncpg==0.29.0
psycopg2-binary==2.9.9

# API interna (health + disparo manual de jobs)
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Motor de cálculo (séries temporais)
pandas==2.2.2
numpy==1.26.4

# Configuração
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Logs estruturados
structlog==24.1.0
